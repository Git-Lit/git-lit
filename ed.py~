import os

class EdRepo():

	def __init__(self, name):
		self.name = name
		self.directory =
		self.edrepo = name

	def mkdir(self, edrepo):

		if not os.path.exists(edrepo):
			os.makedirs(edrepo)
		else:
			print("The folder already exists") 
		
